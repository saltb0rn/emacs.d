(defvar path-to-save "" "The path to save the content of scratch-buffer")

(defun auto-save-scratch-buffer ())

(defun get-content-of-scratch-buffer ())

(defun restore-scratch-buffer-for-the-last-time-saving ())

(provide 'scratch-saving)

